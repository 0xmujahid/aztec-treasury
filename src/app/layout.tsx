import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Aztec Treasury | Hidden Treasure Cryptocurrency",
  description: "Discover the hidden treasure of Aztec cryptocurrency - A modern, engaging crypto project with unique tokenomics and community-driven vision.",
  keywords: ["cryptocurrency", "aztec", "hidden treasury", "blockchain", "tokens"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
